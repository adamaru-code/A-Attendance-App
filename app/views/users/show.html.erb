<% provide(:title, @user.name) %>
<div>
  <table class="table table-bordered table-condensed user-table">
    <tr>
      <td>時間管理表</td>
      <td colspan="2">指定勤務開始時間<span><%= format_basic_info(@user.designated_work_start_time) %></span></br>
          指定勤務終了時間<span><%= format_basic_info(@user.designated_work_end_time) %></span></td>
      <td colspan="2">基本時間<%= format_basic_info(@user.basic_work_time) %></td>
      <td>初日</td>
    </tr>
    <tr>
      <td>所属：<%= @user.affiliation.present? ? @user.affiliation : "未所属" %></td>
      <td>名前：<%= @user.name %></td>
      <td>コード</td>
      <td>0<%= @user.id %></td>
      <td>出勤日数</td>
      <td>締め</td>
    </tr>
  </table>
</div>